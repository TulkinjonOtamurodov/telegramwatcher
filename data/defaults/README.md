This directory holds the shipped default keywords and settings.
The live files data/keywords.txt and data/watcher_settings.json are created
from these on first run and are git-ignored so deploys never conflict.
