# MAKIMA runtime dependencies. Pinned so a rebuild is reproducible.
telethon==1.36.0
python-dotenv==1.0.1

# Optional: a C implementation of Telegram's crypto, roughly 2-5x faster on
# large media. Not required; uncomment if you want it and the wheel installs
# cleanly on your platform.
# cryptg==0.4.0

# zoneinfo needs an IANA tz database; python:3.12-slim does not ship one, and
# without it every pickup timezone fails as timezone_database_missing.
tzdata==2024.1
